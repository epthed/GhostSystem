name: ghostsystem
channels:
  - conda-forge
  - defaults
dependencies:
  - numba
  - pip
  - python=3.8.*
  - python-socketio
  - sanic
  - pip:
      - esper==1.3
prefix: /home/thorbinator/miniconda3/envs/ghostsystem
